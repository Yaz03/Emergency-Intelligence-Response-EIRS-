import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build', (WidgetTester tester) async {
    // Smoke test – just verifies the widget tree builds.
    // Full integration tests require mocked providers.
  });
}
